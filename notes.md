#Operaors in JavaScript
1. Arithmetic Operators
(+.-,*,/,%,**)
2. Assignment Operators
(=,+=,-=,*=,/=)
3. Relational Operators
(<,>,<=,>=,==,===)
4. Logical Operrators
(&,||,!)
5. Bitwise Operators
(left shift, right shift, &, |)
6. Ternary Operators
((condition)?statement1:statement2)
7. Increment/ Decrement Operators
(--i,++i,i++,i--)
8. Special Operators
(, . ; ??)

Conditional Statements:-
                        Conditional statements are those statements that exexute block of code on the behalf of condition .
                        If condition is true then block of code will be executed.
            1. if 
            2. if-else
            3. else-if
            4. Switch

Loop Controls:-
            1. while
            2. do-while
            3. for

Array:-
       Array is collection of multiple values in JS Array can collect any data type .
       1. Using Literals --> []
       2. Using Constructor (Function) --> Array()

String:-
       String is a sequence of characters.
       Types of string:-
              1. Using single quote (' ')
              2. Using double quotw (" ")
              3. Using Literals ``
Function:-
       Function is block of code which perform specific task when it invoke.
              Types of Function:-
              1. Named Function
              2. Anonymous Function
              3. Arrow Function

DOM:-Document Object Model
BOM:-Document Object Model

Object:-
       Object is real world entity.
       Object is collection of methods and properties.
              Types of create an Object:-
                                   1. Using Literals --- {}
                                   2. Using Constructor --- Object();
       Properties:-
              Properties are key value pair.
       Methods:-
              Methods is a function inside an object.


DOM (Document Object Model) :-
                     Tag ==> Every tags are object.
                     Attributes ==> Properties / Nodes

              Selectors in JS :-
                            1. getElementById
                            2. getElementByClassName
                            3. getElementByTagName
                            4. getElementByname
                            5. querySelector
                            6. queryselectorAll

              Validation :-
                     1. Required (Likhna hi hai)
                     2. Length (kitna likha hona cahhiye)
                     3. Pattern (kitna likh aur kya kya hona chahiye )
                                   Regular Expression
                                   /^  Starting point
                                   []  Range define
                                   {}  Length define karna 
                                   $/  Ending point
                                   +   Concatenate 
                                   d/ check digits
                                   w/ check characters
                                   ?.= validity

                                   /^[a-zA-Z]{3,60}$/
       Email Validation :-
                     With Regular Expression:-
                                   Ex- abhithesmartboy1231@gamil.com
                                   /^[a-zA_Z0-9]+@+[a-zA-Z]+\.[a-zA-Z0-9]{2,30}$/